# Skillboxing

This is a Claude Code plugin. Do NOT manually copy files, create hooks, or write skills.

## To install this plugin

Run these two commands exactly:

```bash
claude plugin marketplace add zenzen-sol/skillboxing
claude plugin install skillboxing
```

This registers the marketplace and installs the plugin with all hooks and skills configured automatically. Do not attempt to recreate the hooks or skills by hand.
